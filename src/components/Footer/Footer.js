import React from 'react';
import { Link } from 'react-router-dom';

const Footer= () => {
    return (
        <header>
            <p>
                footer
            </p>
        </header>
    )
};

export default Footer;